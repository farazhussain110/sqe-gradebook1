# LibraryHub Test Cases

| ID     | Title                                         | Requirement                                                    | Preconditions                                                                                | Steps                                                                                                                       | Expected                                                                                                                  | Priority | Type     |
| ------ | --------------------------------------------- | -------------------------------------------------------------- | -------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------- | -------- | -------- |
| TC-001 | Add book with valid new ISBN                  | `add_book` accepts a valid new ISBN                            | LibraryHub is initialized and the ISBN does not already exist                                | 1. Call `add_book` with a valid title, author, and new ISBN. 2. Check the catalog.                                          | The book is added successfully with the correct details and available copy count.                                         | High     | Positive |
| TC-002 | Add book with duplicate ISBN                  | `add_book` must reject duplicate ISBNs                         | A book with the same ISBN already exists                                                     | 1. Call `add_book` using an ISBN already in the catalog. 2. Check the catalog.                                              | The operation is rejected with the expected error and no duplicate book is created.                                       | High     | Negative |
| TC-003 | Add book with malformed ISBN                  | `add_book` validates ISBN format                               | LibraryHub is initialized                                                                    | 1. Call `add_book` with a malformed ISBN such as an invalid-length or non-numeric ISBN. 2. Observe the result.              | The operation is rejected with a validation error and the invalid book is not added.                                      | High     | Negative |
| TC-004 | Borrow book when copies are available         | `borrow_book` allows borrowing when copies are available       | A registered member exists and the selected book has at least one available copy             | 1. Select an available book. 2. Call `borrow_book` for the member. 3. Check the book and member records.                    | The book is successfully borrowed and the available copy count decreases by one.                                          | High     | Positive |
| TC-005 | Borrow book when no copies are available      | `borrow_book` rejects unavailable books                        | A registered member exists and the selected book has zero available copies                   | 1. Attempt to borrow the unavailable book. 2. Check the result and book record.                                             | The borrowing operation is rejected with the expected error and the book state remains unchanged.                         | High     | Negative |
| TC-006 | Return book currently on loan                 | `return_book` returns a book borrowed by the member            | The member has currently borrowed the selected book                                          | 1. Call `return_book` for the member and book. 2. Check the member and book records.                                        | The loan is removed, the book becomes available again, and the available copy count increases by one.                     | High     | Positive |
| TC-007 | Return book not on loan by that member        | `return_book` rejects a book not borrowed by the member        | The book exists but the specified member does not currently have it on loan                  | 1. Call `return_book` for the member and book. 2. Observe the result.                                                       | The operation is rejected with the expected error and no loan or copy count is incorrectly changed.                       | High     | Negative |
| TC-008 | Member borrows at allowed limit               | Members may borrow up to the configured borrowing limit        | A registered member has no current loans and enough available books exist to reach the limit | 1. Borrow books repeatedly until the configured limit is reached. 2. Check the member's active loans.                       | All permitted loans succeed and the member has exactly the allowed maximum number of active loans.                        | High     | Boundary |
| TC-009 | Member borrows beyond allowed limit           | Members cannot exceed the configured borrowing limit           | A registered member is already at the maximum borrowing limit                                | 1. Attempt to borrow one additional available book. 2. Observe the result.                                                  | The additional borrowing is rejected with the expected limit error and the number of active loans remains unchanged.      | High     | Negative |
| TC-010 | Fine calculation for zero days overdue        | No fine is charged when a book is returned on time             | A member has a loan with zero overdue days                                                   | 1. Calculate the fine with `0` overdue days.                                                                                | The calculated fine is exactly **0**.                                                                                     | Medium   | Positive |
| TC-011 | Fine calculation for mid-range overdue period | Fine calculation applies the correct overdue rate              | The fine rules are configured and a book is overdue by a mid-range number of days            | 1. Calculate the fine for a mid-range overdue period, such as 10 days. 2. Compare the result with the configured fine rule. | The calculated fine exactly matches the applicable overdue-tier formula.                                                  | Medium   | Positive |
| TC-012 | Fine calculation at overdue-tier boundary     | Fine calculation changes correctly at an overdue-tier boundary | The fine rules contain an overdue boundary                                                   | 1. Calculate the fine for the final day of one tier. 2. Calculate it again for the first day of the next tier.              | The first calculation uses the lower tier and the second uses the correct next tier, with the expected boundary behavior. | High     | Boundary |

